"use strict";
var _a, _b;
{
    //
    const age = 12;
    const ageBro = 50;
    if (age > 19) {
        console.log("hello world this is me ");
    }
    else {
        console.log("hello there is nothing happen ");
    }
    const a = ageBro > 40 ? "good boy" : "not good";
    console.log(a);
    const res = null;
    const rs1 = res !== null && res !== void 0 ? res : "guest";
    console.log(rs1);
    const user = {
        name: "zubi",
        role: 12,
        address: {
            per: "54 fin",
        },
    };
    const per = (_b = (_a = user === null || user === void 0 ? void 0 : user.address) === null || _a === void 0 ? void 0 : _a.pse) !== null && _b !== void 0 ? _b : "there is no address";
    console.log(per);
    //
}
